
<div class="header-wrapper">
    <header class="grid-header bg-white shadow-lg">
        <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid grid-cols-1 grid-rows-[auto_auto] lg:hidden">
            <a href="" class="large_logo h-12"><span class="invisible">Included</span></a>
            <div class="py-4 flex flex-wrap justify-center space-x-6 row-start-2">

            </div>
            {# {% for key, item in menu %}
                <a href="{{ item.url | url }}">{{item.name}}</a>
            {% endfor %}  #}
        </nav>
        <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid-cols-3 hidden lg:grid">
            <a href="" class="large_logo row-start-1 col-start-2 col-span-1"><span class="invisible">Included</span></a>
            <div class="py-4 flex flex-wrap justify-center space-x-6 col-start-1 col-span-1">

            </div>
        </nav>
    </header>
</div>